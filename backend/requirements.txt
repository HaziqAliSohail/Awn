# Pinned to versions with prebuilt wheels for Python 3.12 (Docker) AND 3.14.
# Verified: app imports, routes register, auth gating + error envelope work,
# unit tests pass.
fastapi==0.141.1
uvicorn==0.52.4
httpx==0.28.1
PyJWT==2.13.0
cryptography>=43.0.0
anthropic==1.4.0
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0

# Optional production speedups (Linux/Docker only — add if desired):
#   uvicorn[standard]==0.52.4
